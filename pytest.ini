[pytest]
# Configuración de pytest para tests del proyecto
# -v: Modo verbose (salida detallada)
# testpaths: Directorio donde se encuentran los tests
# filterwarnings: Ignora advertencias de deprecación

addopts = -v
testpaths = tests
filterwarnings =
    ignore::DeprecationWarning
